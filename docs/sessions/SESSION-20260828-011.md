# SESSION-20260828-011 — Priorización de la siguiente etapa

## Estado

Active — evaluación v1 y cortes P0/P1/P2 aplicados y desplegados técnicamente; pendiente UAT histórico/autenticado y el corte controlado de datos antes de declarar adopción formal.

## Objetivo

Ingerir, comprender, analizar y documentar la exportación operativa de WhatsApp entregada por el propietario; contrastar sus patrones con el aplicativo actual sin convertir evidencia informal en datos o reglas autoritativas.

## Punto de partida

- La sesión anterior (`SESSION-20260813-010`) quedó pausada después de entregar y desplegar el primer corte transversal de experiencia comprensible.
- Producción publicada en `https://rt-sitram-centro-control.vercel.app`; esta sesión promovió el corte P0/P1/P2 después de sus validaciones técnicas.
- Continúa pendiente el UAT visual autenticado con Gerencia/Administración para Evaluador de Viajes y configuración de odómetro GPS.
- OCR permanece en pausa hasta contar con muestras autorizadas y verdad de campo; no se abre la Etapa 1 ni se integra un motor sin satisfacer el gate de la Etapa 0.
- El corte P1 está activo en esta sesión; su implementación no autoriza aplicar
  migraciones, ejecutar pruebas remotas ni desplegar sin autorización expresa.

## Alcance inicial

- Recuperar el contexto mínimo de gobierno y los pendientes vigentes.
- Preservar el ZIP y su extracción como evidencia local ignorada por Git.
- Construir documentación sanitizada e índices trazables bajo `docs/analisis_operativo/`.
- Comparar patrones observados con contratos y capacidades verificables del aplicativo.
- Preservar los cambios existentes del árbol de trabajo.

## Decisiones

- DEC-037 — Captura dual, fondo operativo y ciclos P1 (Accepted, 2026-08-29).

## Log

### 2026-09-01 — Preparación para inicio real y corrección de Reportes

- El propietario autorizó iniciar el corte de datos de DEC-027 y solicitó conservar exclusivamente los tres vehículos reales; todos los demás datos actuales son ficticios/no representativos de la operación real.
- Se confirmó que el corte no puede ejecutarse mediante `db reset`: exige inventario, evidencia/respaldo cuando corresponda, eliminación selectiva y verificación posterior, conforme a DEC-027.
- La sesión del Supabase CLI local devolvió `403` al consultar el proyecto remoto. Se comprobó además el plugin de Supabase solicitado por el propietario: está registrado y hereda permisos de bajo riesgo, pero no expone operaciones de datos en esta sesión. Por seguridad, no se ejecutó ningún borrado remoto sin inventario verificable ni acceso autorizado efectivo.
- Se corrigió Reportes para retirar el panel y la clasificación “Calidad de los datos” entre Confirmados y Provisionales. La superficie deja de presentar como incertidumbre de operación los registros de demostración; mantiene “No disponible” únicamente cuando no existen datos suficientes para un cálculo.
- Validación: 288 pruebas web pasaron; build productivo pasó. Se publicó Vercel `dpl_GYNsem7A4au3i4ahyr49SfVAPPg8`, Ready y asociado a `https://rt-sitram-centro-control.vercel.app`.

### 2026-09-01 — Ejecución del corte controlado DEC-027

- El propietario inició sesión nuevamente en Supabase; la CLI confirmó el proyecto remoto vinculado `rt-sitram-centro-control` y migrations local/remota alineadas.
- El inventario confirmó seis vehículos: BPF-218, DMC-461 y FRT-702 eran unidades de demostración; se preservaron únicamente X3N-719, X2Y-756 y VDR-768. Se mantuvieron también empresa y cinco perfiles de acceso para no interrumpir el ingreso administrativo.
- Se creó el respaldo privado local `evidence/cutover-20260901/public-before-cleanup.jsonl` antes del borrado. La descarga estándar `supabase db dump` no estuvo disponible porque requiere Docker; el respaldo se obtuvo mediante consulta JSON autenticada y quedó con 314879 bytes.
- Se retiró el único archivo ficticio con la API de Storage; no se usó SQL directo sobre tablas de Storage. El corte SQL se ejecutó de forma transaccional mediante `supabase/scripts/controlled-production-cutover.sql`. Al encontrar el log append-only de carga, se usó `TRUNCATE` únicamente después de confirmar que esta era la única empresa y no había eventos de otra empresa; no se desactivó ningún trigger ni política.
- Verificación remota: 0 viajes, clientes, conductores, rutas, documentos, archivos, finanzas, órdenes de trabajo, eventos de viaje/carga, posiciones GPS, enlaces GPS y proyecciones GPS; las tres unidades conservadas quedaron `available` con sus odómetros preservados. Se dejó una única auditoría `CONTROLLED_PRODUCTION_CUTOVER` que referencia el respaldo.
- `supabase db lint --linked --schema public,private --level warning --fail-on warning` reportó una referencia ambigua preexistente en `public.manage_company_profile_access` (`company_id`); no se modificó esa función porque no forma parte del corte de datos.

### 2026-08-28 — Inicio

- El propietario solicitó iniciar sesión.
- Se revisaron `AGENTS.md`, `docs/sessions/current.md`, `docs/sessions/index.md` y `docs/decisions/index.md`.
- Se confirmó que `SESSION-20260813-010` estaba pausada y que no existía una vertical activa por defecto.
- Se abrió `SESSION-20260828-011` como sesión activa de priorización, sin modificar implementación ni reactivar OCR.

### 2026-08-28 — Ingesta y análisis de WhatsApp

- El propietario entregó una exportación ZIP local de un chat operativo y autorizó su extracción, análisis y documentación.
- Se verificó el ZIP antes de extraerlo: 211 archivos, sin rutas inseguras, SHA-256 `4fd934ab4dcd43b32cfd7a91b9c4918ef260fe79b1e9e56b8670298f874199c6`.
- Se conservó una copia idéntica y una extracción local aislada bajo `evidence/whatsapp-trans-sitram-20260828/`, excluida de Git. La copia conserva el mismo SHA-256 que la fuente.
- Se normalizaron 554 mensajes (sin pérdidas de fecha/hora), se enlazaron 210 adjuntos a sus mensajes de origen y se generaron índices privados de mensajes, medios, PDF, entidades y candidatos de evento.
- Se revisaron localmente 14 PDF (43 páginas; 13 con texto incrustado) sin habilitar OCR sobre fotografías ni integrar un motor en el producto, conforme a DEC-035.
- Se crearon 15 documentos sanitizados bajo `docs/analisis_operativo/`, incluidos custodia, metodología, inventario, roles, procesos AS-IS, catálogo de datos, trazabilidad, comparación de brechas y backlog.
- La comparación confirma cobertura de esquema para viajes, combustible, adelantos, gastos/rendiciones, mantenimiento, documentos, facturación y pagos; deja como decisiones pendientes la frontera planilla/rendición, ciclos por tramos, alias maestros y entidades emisoras.
- Se identificó una credencial GPS compartida históricamente en el chat. El propietario confirmó que ya fue cambiada; no se usó, no se reprodujo y quedó registrada sólo como práctica a prevenir.
- Validación: `git diff --check`, revisión de 15 documentos, comprobación de enlaces internos y búsqueda de alias/fragmentos sensibles en documentación versionable: PASS.

### 2026-08-28 — Síntesis de comprensión basada en evidencia real

- A solicitud del propietario, se creó `14_sintesis_comprension_negocio_evidencia_real.md` como síntesis construida exclusivamente desde el corpus de WhatsApp, con separación explícita entre hechos documentados, patrones, inferencias y límites.
- Se actualizó `docs/09_sintesis_comprension_negocio.md` sin reemplazar su comprensión general: se incorporaron únicamente confirmaciones y matices respaldados por la evidencia 2025–2026.
- La actualización reconoce a WhatsApp como una bitácora administrativa concentrada en un registrador predominante, matiza la asignación conductor–unidad y diferencia fondo operativo, rendición y planilla.
- Se dejó expresamente no corroborada por el corpus la utilidad promedio por viaje y no se infirieron políticas comerciales, laborales, contables ni de cierre.
- Se actualizó `docs/00_MANIFIESTO.md` para conservar la huella histórica del paquete original y registrar la revisión vigente del documento 09.
- Validación: enlaces internos, `git diff --check`, huella vigente del documento 09 y búsqueda de fragmentos sensibles: PASS.

### 2026-08-28 — Evaluación del aplicativo con escenarios históricos

- Se formalizó el uso del corpus como referencia histórica y banco de pruebas,
  sin convertir mensajes en reglas ni datos productivos.
- Se crearon `15_marco_uso_evidencia_para_evaluar_aplicativo.md`,
  `16_banco_escenarios_historicos_sanitizados.md`,
  `17_matriz_cobertura_adaptacion_aplicativo.md` y
  `18_plan_pruebas_adaptacion_aplicativo.md`, y se actualizaron los índices,
  la comparación preliminar y el backlog.
- Los 16 escenarios se clasificaron provisionalmente: 1 con capacidad cubierta
  y prueba histórica pendiente, 11 parciales, 1 no cubierto sujeto a decisión
  de alcance y 3 prácticas que no deben trasladarse automáticamente.
- El veredicto global es **adaptación parcial**. El núcleo de viajes, autoridad
  del backend, RLS, idempotencia, offline, Storage privado y rendición explícita
  debe mantenerse.
- La brecha dominante es actor/momento: el corpus es una bitácora centralizada,
  mientras la PWA reserva combustible y gastos nuevos al Conductor con viaje
  activo. Administración revisa, pero no dispone de alta gobernada en la UI.
- Se identificó un P0 de integridad financiera: `record_expense` admite a
  personal insertar un gasto nuevo en un viaje con rendición cerrada; el guard
  protege `UPDATE/DELETE`, no `INSERT`. No se modificó el backend en esta tarea.
- Se confirmó un P1 de evidencia: la UI conserva `file_id`/`hasFile`, pero no
  ofrece abrir o descargar comprobantes y documentos privados para revisarlos.
- Otras brechas parciales incluyen ciclos sin superficie, estado de adelantos
  no completado por la rendición, mantenimiento reducido, detracción/entidad
  emisora ausentes, una sola evidencia por actividad y cierre financiero sin
  recorrido completo.
- La línea base local pasó `pnpm format:check`, `pnpm lint`, `pnpm typecheck`,
  386 pruebas Vitest, el verificador de invitaciones y `pnpm build`. Los 9
  archivos pgTAP fueron inspeccionados, no ejecutados contra Supabase.
- La pantalla pública de acceso cargó localmente sin errores visibles ni de
  consola. No se ejecutó UAT visual autenticado ni se usaron credenciales.
- No se importaron mensajes, archivos, saldos, alias ni transacciones al
  aplicativo; toda la evaluación versionable permanece sanitizada.

### 2026-08-29 — Correcciones implementables derivadas de la evaluación

- Se actualizó `AGENTS.md` para referenciar
  `docs/analisis_operativo/corpus_whatsapp_ingesta_20260828/00_indice.md`
  cuando una tarea requiera comprender, contrastar, probar o evolucionar el
  flujo operativo. La referencia aclara que el corpus es evidencia histórica,
  no una fuente que sustituya decisiones aceptadas ni reglas autorizadas.
- Se creó la migración
  `20260829113558_enforce_closed_settlement_expense_inserts.sql` para completar
  DEC-006 y DEC-020: el guardia de `expenses` ahora cubre `INSERT`, `UPDATE` y
  `DELETE`, toma `NEW` al insertar y conserva el bloqueo con `FOR KEY SHARE`.
  Un gasto nuevo no puede entrar a una rendición cerrada por RPC ni por el
  permiso directo de personal; el reintento idempotente exacto y la reapertura
  autorizada siguen funcionando conforme al contrato existente.
- Se amplió `backend_invariants.test.sql` de 54 a 56 aserciones con los casos
  de gasto nuevo vía `record_expense` y `INSERT` directo después del cierre. El
  harness `scripts/verify-p1-hardening.mjs` incluye la nueva migración para no
  evaluar la versión anterior del trigger.
- Se habilitó la consulta de evidencia privada en Administración para gastos,
  adelantos, documentos y las secciones con evidencia del detalle de viaje.
  La UI transmite únicamente `fileId`; el gateway resuelve metadata bajo RLS y
  usa `Storage.download()` autenticado. El archivo se visualiza como Blob
  temporal (PDF con `iframe` aislado; JPEG/PNG/WebP como imagen), se revoca al
  cerrar o perder conexión y nunca se publica ni se ofrece desde la copia
  offline.
- Se añadieron pruebas de gateway para filtro por `fileId`, MIME permitido,
  metadata no visible, error de Storage, estado offline y ausencia de ruta de
  Storage en las filas administrativas. Se aplicó `overflow-wrap` al encabezado
  del diálogo para nombres de archivo largos.
- Validación completada: `pnpm format:check`, `pnpm lint`, `pnpm typecheck`,
  `pnpm test` (392 pruebas), `pnpm build`, pruebas enfocadas de Administración
  (38), `node --check scripts/verify-p1-hardening.mjs` y `git diff --check`.
  La build de Vite terminó correctamente; conserva una advertencia existente
  de chunks grandes, sin introducir un error de compilación.
- No se aplicaron migraciones ni pruebas contra Supabase remoto. pgTAP no pudo
  ejecutarse porque el host no tiene Docker, Podman ni una base local activa.
  El UAT visual autenticado sigue pendiente: el ejecutable `agent-browser` no
  está disponible en este host y no se usaron credenciales reales.

### 2026-08-29 — Orientación a producto desplegable y ejecución eficiente

- El propietario confirmó que la siguiente etapa debe orientarse a completar
  la aplicación para uso y despliegue, no a mantener el entorno local como
  condición de avance. Se actualizó `AGENTS.md` para priorizar cortes
  verticales verificables que incluyan dominio, backend/RLS, migración, UI,
  offline, pruebas y documentación.
- La falta de infraestructura local permite avanzar con construcción y
  verificación disponible, pero no autoriza omitir validaciones relevantes,
  debilitar seguridad ni aplicar cambios remotos sin autorización.
- Se añadió una práctica de ejecución de agentes basada en contexto selectivo,
  evidencia de herramientas/pruebas, separación de descubrimiento-plan-
  implementación-verificación, especialización solo para subtareas
  independientes y criterios de parada explícitos.
- Se fijó un límite operativo de menos de 200 líneas para el `AGENTS.md` raíz:
  concentra reglas permanentes y portables; las instrucciones especializadas
  se mantienen bajo demanda en skills o documentos autoritativos enlazados.
- Se creó `docs/mapa_repositorio.md` para dirigir cada tipo de tarea hacia su
  módulo, entrada técnica, pruebas y contexto complementario. El mapa evita
  inventariar archivos individuales, dependencias, builds y temporales.
- `implementation/scripts/repo-map.mjs` descubre aplicaciones, paquetes,
  features, librerías y superficies de backend. `pnpm repo-map:update` regenera
  el inventario y `pnpm repo-map:check` detecta deriva; la comprobación quedó
  integrada en `format:check` para que forme parte de la validación habitual.
- Se corrigieron ambigüedades de `AGENTS.md`: el plan pausado ya no se confunde
  con el plan de una tarea, se corrigió “las subtareas” y un cambio validado
  dentro de alcance puede considerarse terminado.

### 2026-08-29 — Corte P1: captura dual, fondos y ciclos

- El propietario aceptó DEC-037: Conductor conserva captura offline-first;
  Administración y Gerencia pueden registrar en línea gastos y combustible en
  representación, con actor autenticado, fecha real, motivo e auditoría.
- Se implementaron contratos y migraciones P1 para captura de gasto/combustible
  por personal, cierre/reapertura, inmutabilidad de combustible tras cierre y
  fondos operativos rendidos al cierre. El combustible tardío es costo del viaje
  y no modifica por sí solo el saldo de rendición del Conductor.
- Se implementó el recorrido administrativo de captura y el de ciclos
  operativos por unidad. Cada servicio conserva su viaje, facturación y
  rendición; el ciclo no fusiona dinero ni cierres.
- DEC-037 mantiene planilla fuera del producto y prohíbe importación,
  inferencia o correspondencia automática de alias históricos.
- Se versionaron pruebas pgTAP y contratos de RPC para el corte P1. Los conteos
  estáticos revisados son 39/39 (finanzas), 25/25 (ciclos), 56/56
  (invariantes), 155/155 (GPS) y 70/70 (RPC).
- Validación local completada: `pnpm format:check`, `pnpm lint`, `pnpm
  typecheck`, las cinco suites Vitest (405 pruebas), `pnpm build`,
  `node --check scripts/verify-p1-hardening.mjs` y `git diff --check`.
  La build conserva sólo la advertencia informativa existente de chunks grandes.
- No se aplicó ninguna migración ni se ejecutó pgTAP contra una base local o
  remota; el host no dispone de Docker. La validación de migraciones/pgTAP y el
  UAT histórico sintético/autenticado siguen pendientes.

### 2026-08-29 — Corte P2: mantenimiento detallado y evidencia privada

- El propietario aceptó DEC-038: una orden sin líneas puede cerrar con monto
  global manual de repuestos; con líneas, el total global debe coincidir con
  la suma de cada importe redondeado a céntimos. La evidencia es opcional,
  múltiple, privada y nunca infiere diagnóstico, trabajos, costos, repuestos
  ni transición de estado.
- Se añadieron comandos idempotentes y auditados para crear/avanzar órdenes,
  registrar líneas de repuesto y asociar evidencia. Las escrituras directas de
  órdenes, líneas y asociaciones se revocaron para el usuario autenticado;
  las funciones usan empresa, rol y archivos de la misma empresa. El cierre
  conserva la autoridad GPS de odómetro y bloquea la orden antes de totalizar
  líneas para evitar carreras.
- La nueva tabla `work_order_evidence` tiene RLS forzado, lectura explícita
  sólo para personal y asociación mediante RPC. La concesión explícita de
  `SELECT` considera el cambio vigente de Supabase que deja nuevas tablas de
  `public` fuera de la Data API si no reciben permiso explícito.
- Administración y Gerencia cuentan con una pantalla online-only de detalle
  de orden: avance separado del cierre, maestro y líneas de repuesto, costo
  conciliado y archivos privados revisables. El mensaje de cierre aclara que
  el total mostrado es orientativo y el servidor calcula y valida el importe
  final; no se simula una copia offline de mantenimiento.
- Se agregaron `p2_maintenance_controls.test.sql` (47/47 aserciones estáticas)
  y los cuatro contratos P2 a `rpc_contract.test.sql` (74/74). Se actualizaron
  los escenarios históricos y el plan UAT para cubrir monto manual sin líneas,
  suma por líneas, precisión, idempotencia, doble evidencia, aislamiento y no
  inferencia.
- Validación local completada: `pnpm format:check`, `pnpm lint`, `pnpm
  typecheck`, `pnpm test` (410 pruebas), `pnpm build` y `git diff --check`.
  La build conserva sólo la advertencia informativa existente de chunks grandes.
- No se aplicó ninguna migración ni se ejecutó pgTAP: `pnpm exec supabase
  status --output json` confirmó que este host no tiene Docker ni Podman. No
  se usó el proyecto remoto enlazado porque no es verificablemente el proyecto
  de R&T y no hay autorización para migrar una base distinta. Sigue pendiente
  el UAT visual autenticado.

### 2026-08-29 — Validación remota y despliegue técnico P0/P1/P2

- El propietario autorizó el despliegue. Se verificó el proyecto Supabase de
  R&T enlazado y se aplicaron las siete migraciones P0/P1/P2, incluidas dos
  correcciones de liberación: las cuatro RPC de ciclos ahora evitan la
  ambigüedad entre variable y columna `company_id`, y la RPC de repuestos no
  deja un local sin uso que active el lint estricto. Las firmas, permisos y
  semántica de dominio se conservaron.
- Como este host sigue sin Docker, el comando oficial `supabase test db` no
  pudo arrancar su contenedor local. En su lugar, las 12 suites pgTAP se
  ejecutaron una por una contra el remoto autorizado mediante `db query`; cada
  suite declara `begin` y `rollback`, por lo que no dejó fixtures ni cambios.
  Todas pasaron. También pasaron `db lint` al nivel warning y el verificador
  remoto de ACL de funciones dentro de una transacción revertida.
- PowerSync validó el stream vigente sin cambio de publicación; el estado
  posterior quedó conectado, con réplica inicial terminada, sin errores y sin
  rezago. P1/P2 continúan online-only donde DEC-037/DEC-038 así lo establecen;
  no se amplió el stream para simular sincronización de mantenimiento.
- Se configuró en Vercel la configuración pública requerida por la PWA y se
  añadió `.vercelignore` para impedir que el `.env.production` local sea parte
  del artefacto desplegado. La candidata `dpl_7Dtk1gwnPBpBCazWUoC5ufF9eaWS`
  compiló correctamente, se verificó su shell/manifest/service worker y se
  promovió a `https://rt-sitram-centro-control.vercel.app`.
- La comprobación autenticada y sólo de lectura de Inicio, Ciclos operativos y
  Mantenimiento confirmó sesión conectada, datos cargados y ausencia de errores
  de aplicación. No se creó ni modificó información operativa durante el humo.
- La validación local final pasó `pnpm format:check`, `pnpm lint`, `pnpm
  typecheck`, `pnpm test` (410 pruebas) y `pnpm build`. La advertencia ya
  existente de chunks grandes de Vite sigue siendo informativa y no bloqueó la
  compilación.
- P3 se trató como endurecimiento de liberación: los cambios de lenguaje deben
  surgir del UAT, las métricas de adopción/uso paralelo de WhatsApp requieren
  definición y dueño antes de recoger eventos, y sólo reglas ya aprobadas se
  automatizan. No se inventaron métricas ni reglas de negocio para declarar un
  cierre artificial.
- La auditoría final confirmó que el `HEAD` sigue en el commit inicial y el
  árbol conserva cambios sin versionar. No se creó un commit amplio ni una
  etiqueta sobre un alcance que no era aislable con seguridad. Antes de una
  certificación de uso formal se debe revisar, versionar y etiquetar el estado
  exacto desplegado para contar con rollback reproducible.

### 2026-08-29 — Manual ejecutivo para dirección

- Se elaboró `output/pdf/Manual_Ejecutivo_Centro_de_Control_Digital_RT_SITRAM.pdf`, dirigido al propietario. El PDF explica la operación desde la práctica vigente de WhatsApp hacia el registro estructurado, cubre las pantallas por grupos funcionales y roles, y explicita los límites de alcance que no deben presentarse como capacidades cerradas.
- El manual contiene 29 páginas con portada, flujo de servicio, matriz de roles, inventario completo de pantallas, una guía visual orientativa por pantalla o grupo, rutina de dirección, comparación WhatsApp/sistema y fuentes. No contiene datos reales, credenciales, placas, adjuntos ni capturas de producción.
- Se verificó mediante renderizado a PNG de todas las páginas y revisión visual por montajes; se confirmó jerarquía, legibilidad, continuidad de encabezados/pies, diagramas y ausencia de desbordes. `pdfinfo` confirmó 29 páginas y los metadatos formales del documento.

### 2026-08-29 — Suspensión de telemetría GPS en piloto

- El propietario suspendió las fuentes GPS de todas las unidades del piloto y confirmó que la interfaz no debe mostrar una última señal, velocidad, ignición u odómetro GPS como datos operativos mientras una fuente está suspendida.
- Se aceptó DEC-039: la suspensión preserva evidencia, historial y auditoría, pero oculta la proyección contextual de telemetría. No desvincula la unidad, no borra datos y no modifica el estado operativo ni financiero.
- Se añadió una migración que aplica el límite de visibilidad en servidor sin exponer a Administración la tabla de configuración de Gerencia; la tarjeta de contexto se omite cuando no existe fuente GPS activa.
- Se aplicó `20260829160000_hide_suspended_gps_telemetry.sql` en el Supabase remoto autorizado. La prueba pgTAP `gps_telemetry.test.sql` pasó con 41 controles, incluido el caso de fuente suspendida ausente de la vista contextual.
- La PWA fue publicada en producción con el despliegue `rt-sitram-centro-control-ams4s3em0-jotures-projects.vercel.app`; el dominio canónico respondió HTTP 200. La comprobación agregada del remoto confirmó 3 vínculos preservados, 3 fuentes suspendidas y 0 filas de telemetría visibles.
- Se corrigió además el tablero: ante una flota sin fuentes GPS activas, no se interpreta la proyección vacía como tres vínculos faltantes ni se muestra el panel “Telemetría por revisar”.

### 2026-08-30 — Corte UX administrativo: Inicio, Viajes y Flota

- Se mejoraron las pantallas administrativas sin cambiar migraciones, RPC,
  RLS, publicación de PowerSync, estados ni reglas de negocio. Viajes ahora
  prioriza por etapa, ofrece búsqueda y vistas URL (`vista` y `q`), presenta
  la ruta como dato principal y resuelve una acción única según el estado.
- Inicio convierte la atención prioritaria y el trabajo diario en navegación
  accionable, compacta los indicadores operativos y transforma la flota en un
  resumen que lleva a cada unidad. Flota prioriza atención/operación/
  disponibilidad, ofrece selección semántica y reemplaza el detalle de unidad
  por un centro operativo de sólo lectura contextual.
- Se añadieron las proyecciones y contratos de UI `AdminVehicleRow`,
  `AdminVehicleDetail` y `loadVehicleDetail`. La ficha utiliza datos locales
  disponibles para unidad, viajes y odómetro; mantenimiento, documentos,
  alertas y GPS se identifican como dependientes de conexión y se conserva el
  límite de DEC-039 para la telemetría suspendida.
- Validación local: `pnpm format:check`, `pnpm lint`, `pnpm --filter
  @rt-sitram/web typecheck`, build y pruebas. Las suites de paquetes sumaron
  145 pruebas aprobadas; la web aprobó 58 archivos y 272 pruebas al ejecutarse
  serialmente (`--maxWorkers=1`), más el contrato de invitación. La ejecución
  paralela de la suite web no llegó a emitir casos en este host, por lo que no
  se usó como señal de aprobación. La build conserva sólo la advertencia
  informativa existente de chunks grandes.
- La Preview `dpl_8kaC1B7961wcaXFYw9m8184ekddz` quedó lista y se promovió al
  artefacto de producción `dpl_5hPftRv2oFG1Hu6BkeH2miyHs6J7`, accesible en
  `https://rt-sitram-centro-control.vercel.app`. La inspección de Vercel
  confirmó estado Ready, alias canónico y ausencia de logs posteriores. El
  humo HTTP autenticado y la inspección visual automatizada no pudieron
  realizarse desde este host: la Preview queda tras SSO de Vercel y el puente
  del navegador integrado no pudo inicializarse. No se realizaron mutaciones
  de datos durante la verificación.

### 2026-08-30 — Corte integral de comprensión e interactividad

- El propietario autorizó el plan integral y se aceptó DEC-040: las pantallas
  administrativas presentan primero ruta, persona, unidad, cliente, trabajo o
  dinero; los UUID y folios permanecen como referencias secundarias. Inicio,
  búsqueda, viajes, rendiciones, flota, conductores, clientes, documentos,
  mantenimiento, combustible, proveedores y perfil adoptan acciones visibles,
  filtros URL y contexto humano por registro.
- Se aplicaron en el Supabase remoto autorizado las migraciones aditivas
  `20260830183507_comprehension_interactivity_integral.sql` y
  `20260830195000_add_document_attachment_concurrency.sql`. La primera añade
  la modalidad/tarifa de flete y comandos auditados de maestro, disponibilidad,
  proveedor y adjunto; la segunda añade concurrencia `updated_at` al documento
  antes de permitir su adjunto auditado. La RPC histórica de viaje conserva su
  firma compatible y el servidor recalcula la tarifa por tonelada.
- Las trece suites pgTAP se ejecutaron contra el remoto dentro de
  transacciones con `rollback`: 586 controles aprobados. Se verificaron
  aislamiento empresarial, ACL, concurrencia, cálculo/redondeo de flete,
  documentos privados, disponibilidad del conductor, compatibilidad de RPC,
  GPS, finanzas y mantenimiento. `supabase db lint --linked --schema public
  --fail-on error` no encontró errores en el esquema público.
- La validación del cliente pasó `pnpm format:check`, `pnpm lint`, `pnpm
  typecheck`, `pnpm test` (419 pruebas: dominio 48, integraciones 14,
  Goldcar 74, OCR 9 y web 274), el contrato de invitación y `pnpm build`.
  La build generó 21 activos de shell offline; conserva solamente la
  advertencia informativa existente por chunks grandes.
- La Preview final `dpl_3UMzsmDBPrf21aJtpYoqtwEQaq6W` quedó Ready en
  `https://rt-sitram-centro-control-2kv3m0nfg-jotures-projects.vercel.app`.
  Vercel la protege por SSO. La llamada oficial de promoción creó la producción
  `dpl_DVrV5bXPdymb7UEPj3PbGG7ZUDfN`, Ready y con alias canónico
  `https://rt-sitram-centro-control.vercel.app`; no se desplegó un árbol
  distinto entre ambos pasos.
- El humo público de producción confirmó HTTP 200 y shell de aplicación para
  Inicio, Viajes, Flota, Documentos, Mantenimiento, Proveedores y Perfil. El
  manifiesto devolvió `application/manifest+json` con el nombre, alcance e
  iconos esperados; `sw.js` devolvió JavaScript con `Service-Worker-Allowed: /`
  y la caché de shell generada. No se crearon ni modificaron registros de
  negocio durante el humo.
- El UAT visual/autenticado en Preview sigue pendiente: el puente de navegador
  del host falló antes de inicializarse y no se sustituyó por otra superficie.
  La protección SSO devolvió la página de acceso de Vercel a las consultas HTTP
  de Preview. Esta limitación queda explícita y no se presenta como evidencia
  visual de las rutas autenticadas.

### 2026-08-30 — Corrección móvil de acciones en Trabajo de hoy

- El UAT visual del propietario detectó que las acciones `Resolver rendición`
  y `Abrir viaje` heredaban un ancho de icono dentro de sus grids. El texto se
  partía en dos líneas y quedaba fuera de jerarquía en Inicio móvil.
- Se corrigió el layout sin alterar datos ni navegación: en escritorio las
  acciones ocupan una columna propia; hasta 672 px pasan a una fila completa,
  con botón legible, foco visible y altura táctil. Se mantienen ruta, estado y
  contexto antes de la acción.
- Pasaron formato, lint, las 274 pruebas web, el contrato de invitación y la
  build. La Preview `dpl_7SKHrwkH4WyJYhPqF2LR22oySnL4` se promovió a la
  producción `dpl_CtEHHQXEtMWvJaMEc1m378hL85f7`. El dominio canónico entrega
  el CSS actualizado; no se realizaron mutaciones de datos operativos.

### 2026-08-30 — Implementación local de Reportes y Analítica (DEC-041)

- El propietario aprobó el catálogo completo de Reportes con portada y
  pestañas. Se aceptó DEC-041: el máximo resultado económico es margen directo
  confirmado/provisional, nunca utilidad neta; la utilización usa tiempo en
  servicio; las detenciones sólo muestran tiempo/causa; y carga/vacío se mide
  mediante eventos explícitos, no inferidos. La cobertura comienza con el
  despliegue y no reconstruye historia previa.
- Se añadió `20260830210000_create_reporting_analytics.sql`: eventos
  `trip_load_state_events` append-only e idempotentes con supersesión para
  corrección auditada, línea base e intervalos de estado de unidad, proyección
  de órdenes bloqueantes, vistas `security_invoker`, snapshot de reportes,
  opciones de filtro y registro de exportación. También se añadió pgTAP en
  `reporting_analytics.test.sql`.
- La PWA ahora carga Reportes como feature diferida. La portada y sus ocho
  análisis consumen un único snapshot para gráficos Recharts, tabla accesible,
  CSV protegido contra fórmulas y PDF/dossier diferido. URL conserva periodo,
  pestaña, filtros y comparación. Contabilidad recibe únicamente Cobranza;
  offline muestra que el cálculo requiere servidor.
- La captura del Conductor exige la condición de carga al iniciar y permite
  registrar `Cambiar condición de carga` con odómetro durante el tránsito. La
  tabla entra al esquema/stream/upload/reintentos de PowerSync; las escrituras
  iniciales de inicio y condición se encolan de forma atómica.
- Validación local aprobada: formato del alcance, `pnpm lint`,
  `pnpm --filter @rt-sitram/web typecheck`, `pnpm test` (425 pruebas de
  paquetes) y `pnpm --filter @rt-sitram/web build`. La build dejó Recharts en
  el chunk diferido de Reportes y react-pdf en el chunk de exportación. La
  advertencia de chunks grandes ya conocida no bloqueó la build.
- No se levantó Supabase local, no se ejecutó pgTAP ni se hizo UAT autenticado:
  el host no tiene Docker Desktop ni Podman. No se aplicó ninguna migración
  remota ni se creó Preview/producción. La revisión local sólo pudo confirmar
  la pantalla de acceso porque no se usaron credenciales; queda pendiente la
  revisión visual autenticada de 320/390/639/960/escritorio.

### 2026-08-30–31 — Validación remota, datos sintéticos y producción de DEC-041

- Con autorización expresa del propietario se aplicó
  `20260830210000_create_reporting_analytics.sql` en el Supabase remoto
  autorizado. `db lint` no reportó advertencias y `reporting_analytics.test.sql`
  pasó sus 19 controles pgTAP contra el remoto. La falta de Docker/Podman del
  host no afectó estas verificaciones remotas.
- Se creó y ejecutó `implementation/supabase/scripts/seed_reporting_uat.sql`.
  Registra datos sintéticos, auditados e identificables como demostración: tres
  unidades, tres viajes terminados, segmentos cargados/vacíos, combustible
  confirmado y provisional, una orden finalizada y una abierta, y facturas con
  pago parcial/anulado. No se reemplazaron datos operativos reales; el
  propietario confirmó que la empresa no tenía datos reales en el ambiente.
- La verificación autenticada de sólo lectura confirmó que el snapshot remoto
  entrega 3 viajes, 3 consumos, 2 órdenes, 3 documentos de cobranza, 13
  intervalos de unidad y 6 segmentos. Los importes se separan por moneda y se
  aplicó el límite diario de `America/Lima` al cálculo de pagos y a las fechas
  visibles.
- Tras la revisión visual del propietario se corrigieron dos problemas de
  comprensión: los gráficos dejaron de usar el color negro y la leyenda
  genérica; ahora muestran el indicador elegido con verde/cobre/gris y nombres
  operativos. Los códigos de prueba y folios internos se sustituyeron por
  referencias humanas como `Unidad BPF-218` y `Servicio Lima → Huancayo`.
  También se evitó que una fecha UTC de madrugada se muestre como el día
  siguiente para Lima, y se añadió una protección de presentación ante un chunk
  diferido obsoleto.
- Validación final: lint y TypeScript del workspace, 282 pruebas web, build de
  producción y `git diff --check` aprobados. `pnpm format:check` señaló un
  formato preexistente en `AdminRoutePage.tsx`, fuera del alcance; los cuatro
  archivos modificados de Reportes quedaron formateados y comprobados. Una
  ejecución inicial de la suite web agotó el tiempo en una prueba de Dashboard
  no relacionada; al reintentar, los 60 archivos y 282 pruebas pasaron.
- Se construyó y desplegó el mismo artefacto con Vercel. La producción
  `dpl_Bzfv8VRvqprUE3kfUMwgzGCZcibn` quedó `Ready`, el alias canónico
  `https://rt-sitram-centro-control.vercel.app` respondió HTTP 200 para
  `/reportes` y Vercel no registró errores posteriores. La revisión visual
  autenticada se realizó a 639 px; quedan pendientes las demás medidas y el
  UAT representativo por rol antes de declarar uso operativo real.

## Log (continuación)

### 2026-08-31 — Archivo de la skill de diseño

- Por solicitud explícita del propietario, se archivó `skills/rt-sitram-design/` en `docs/archive/skills/rt-sitram-design/`, fuera de las rutas de skills descubribles.
- Se eliminó la guía obligatoria de `AGENTS.md` y las referencias operativas de `docs/mapa_repositorio.md` y de los tres planes de verticales.
- Se preservaron las decisiones y logs históricos —incluida DEC-022— para no modificar la trazabilidad de decisiones ya aceptadas. La identidad visual existente del producto no fue modificada.

### 2026-08-31 — Rediseño integral móvil primero (DEC-042)

- El propietario sustituyó la revisión página por página por un rediseño integral de todas las rutas, detalles y estados visibles de Gerencia, Administración, Contabilidad y Conductor. El límite fue explícito: mejorar experiencia y presentación sin crear tablas, migraciones, RPC, RLS, reglas financieras ni cambios remotos.
- Se consolidaron tokens de superficie, jerarquía, radio, sombra, tipografía, foco y movimiento; el shell ahora aporta contexto de ruta y retorno a la pantalla padre. Un mapa tipado cubre todas las rutas de producto y se prueba contra el registro de rutas sin participar en autorización.
- Inicio, viajes, flota, detalle de unidad, mantenimiento, documentos, GPS, finanzas, reportes y la experiencia de conductor comparten primitivas de acción, listas, formularios, revisión previa y estados honestos. Las placas, códigos operativos, importes y métricas importantes dejaron de usar monoespaciada; ésta queda para diagnósticos o identificadores estrictamente técnicos.
- Se atendieron los hallazgos visuales del propietario: acciones prioritarias menos pesadas, flota móvil con una columna de avance alineada, hero de unidad con jerarquía visible y eliminación de la tipografía técnica donde restaba calidad percibida.

#### Auditoría formal de movimiento

| Área | Antes | Después | Veredicto |
|---|---|---|---|
| Navegación y cambio de ruta | Transiciones de margen/ancho del shell y fundido raíz en rutas frecuentes | Sin animación de navegación; el layout cambia de forma inmediata | Aprobado |
| Tableros y configuración | Entradas escalonadas decorativas al abrir superficies operativas | Superficies estáticas; sólo se anima la apertura puntual de disclosure o diálogo | Aprobado |
| Paneles, diálogos y sheets | Entradas breves, con alguna escala `0.99` | 220–280 ms, opacidad y `transform`, escala inicial `0.97` en diálogos | Aprobado |
| Presión y hover | Respuesta desigual y hover disponible para táctil | `scale(0.97)` a 120 ms y hover limitado a `hover:hover` + puntero fino | Aprobado |
| Carga y sincronización | Pulso de sincronización y spinner sin política consolidada | Se mantienen sólo como indicación de estado; respetan reducción de movimiento | Aprobado |
| Rendimiento y accesibilidad | Había propiedades de layout animadas en el shell | Sin `transition: all` ni `scale(0)`; movimiento limitado a opacidad/transform y reducción de movimiento/transparencia activa | Aprobado |

- Verificación local autenticada con el rol de Gerencia y datos sintéticos: Inicio en 320/390/639/960/1440 px, Viajes, Flota, Mantenimiento, Rendiciones, Reportes, detalle de unidad y Odómetro GPS no tuvieron desborde horizontal. En la ficha de unidad `X3N-719` se confirmó una identidad de 32 px y retorno `Volver a Flota`. El menú móvil abrió, cerró con Escape y devolvió el foco al disparador. La navegación inferior se mostró sólo bajo el breakpoint móvil.
- Validación: `pnpm format:check`, `pnpm typecheck`, `pnpm lint`, `pnpm test` (430 pruebas: dominio 48, integraciones 14, Goldcar 74, OCR 9 y web 285, más contrato de invitación) y `pnpm build` pasaron. La build generó 24 activos de shell offline y conservó la advertencia informativa preexistente de chunks grandes.
- No se desplegó, no se aplicaron migraciones ni se alteraron datos. Las validaciones autenticadas de Administración, Contabilidad y Conductor —en particular el recorrido offline de captura y recuperación de Conductor— siguen pendientes y no se presentan como completadas.

### 2026-08-31 — Contexto humano, español y color semántico (DEC-042)

- A partir de las anotaciones del propietario se eliminó la presentación de UUIDs de viajes en Inicio, Gastos, Combustible, Adelantos y Cobranza. Las referencias ahora resuelven ruta, cliente, unidad y, cuando corresponde, vencimiento; los identificadores sólo permanecen en capas técnicas no visibles.
- Se completó la traducción de estados y eventos operativos observados en el UAT, incluidos `not_required`, `unbilled`, `trip_start`, `operational` y solicitudes offline. El detalle del viaje explica expresamente qué representan el estado administrativo y financiero.
- Flota y las tablas compartidas usan color semántico por estado: disponibilidad/confirmación en verde, operación o programación en azul, revisión o mantenimiento en ámbar y vencimiento, rechazo o bloqueo en rojo. Las métricas de viaje usan el mismo código para separar ingreso/distancia, costo y resultado, sin alterar reglas de negocio.
- Gastos y Cobranza añaden el contexto suficiente para decidir: servicio, cliente, unidad, importe, vencimiento y estado. El formulario de plan preventivo incorpora ejemplos y ayudas para nombre, tipo y frecuencia.
- Reportes se rediseñó como superficie de decisión: las barras horizontales mantienen las etiquetas completas, el eje muestra moneda o unidad, cada KPI explica su significado, el gráfico indica su propósito y un segundo gráfico expone la calidad de los datos confirmados, provisionales o faltantes. El Evaluador recibió límites responsive para impedir que su contenido fuerce scroll horizontal.
- Verificación local autenticada con datos sintéticos: Inicio, Gastos y Cobranza muestran referencias humanas; Flota entrega chips `En viaje` azul, `Disponible` verde y `Reparar` ámbar; Reportes y Evaluador no tuvieron desborde a 639 px. La recarga final del navegador para repetir el historial traducido se interrumpió por un fallo transitorio de la webview; la traducción se cubre en la capa común y requiere una repetición breve durante el UAT formal.
- Validación aprobada: `pnpm format:check`, `pnpm typecheck`, `pnpm lint`, `pnpm test` (430 pruebas: dominio 48, integraciones 14, Goldcar 74, OCR 9 y web 285, más contrato de invitación) y `pnpm build`. La build conserva la advertencia informativa de chunks grandes. No se aplicaron migraciones, no se modificaron datos y no hubo despliegue.

### 2026-08-31 — Guías y ejemplos en formularios de toda la operación

- Por solicitud del propietario, el patrón de ayuda ya validado en el plan preventivo se extendió a los formularios de Administración y Conductor. Las primitivas administrativas `Field`, `TextareaField`, `SelectField`, `FileField` y `CheckboxField` ahora resuelven una guía por el significado del dato y presentan ejemplos seguros cuando aplica; los formularios nuevos que reutilicen estas primitivas reciben la misma orientación por defecto.
- La guía cubre importes, flete, comprobantes, rutas, maestros, documentos, mantenimiento, odómetro, combustible, pagos y decisiones auditadas. Los campos no mapeados conservan una explicación honesta y específica por tipo, sin inventar reglas de negocio ni normalizar datos que el servidor debe validar.
- La experiencia de Conductor recibió el mismo tratamiento con ayudas más concisas para uso en ruta: kilometraje, carga, combustible, gasto, incidencia, evidencia, descarte de cola y confirmación de entrega explican qué confirmar y muestran ejemplos de captura. Perfil y acceso mantienen orientación de seguridad sin sugerir contraseñas.
- También se completaron los pocos formularios especializados: cotización de carga/flete, ciclos, evaluador de viajes, GPS y perfil. Los filtros de listas no se modificaron porque no registran hechos ni decisiones de operación.
- Verificación: `pnpm format:check`, `pnpm typecheck`, `pnpm lint`, `pnpm test` (430 pruebas: dominio 48, integraciones 14, Goldcar 74, OCR 9 y web 285, más contrato de invitación) y `pnpm build` pasaron. La comprobación de navegación local confirmó la vista móvil de Mantenimiento; la automatización del disclosure agotó su plazo antes de abrir el formulario y queda para UAT visual breve. No se aplicaron migraciones, no se modificaron datos ni se desplegó.

### 2026-09-01 — Despliegue autorizado de las guías de formularios

- El propietario autorizó explícitamente el despliegue a producción. La prevalidación de Vercel confirmó el proyecto vinculado `rt-sitram-centro-control`, la configuración Vite, el build remoto y el conjunto de archivos a publicar; los archivos de entorno locales quedaron ignorados.
- Se publicó la producción `dpl_9tGxzkoBT8p8ks2QBua52zjFziKE`, Ready en `https://rt-sitram-centro-control-kcpdkbqrs-jotures-projects.vercel.app`, y Vercel reasignó el alias canónico `https://rt-sitram-centro-control.vercel.app` a esta versión.
- El build remoto completó con los mismos 1,013 módulos y la advertencia informativa ya conocida sobre chunks grandes; no hubo errores de compilación. El humo HTTP confirmó 200 para Inicio y `/finanzas/rendiciones`, además de `sw.js` con `Service-Worker-Allowed: /` y `manifest.webmanifest` con MIME correcto.
- No se ejecutaron migraciones, no se modificaron datos, reglas, RLS, RPC, secretos ni PowerSync. El despliegue no sustituye el UAT autenticado por rol, en especial Conductor/offline y los breakpoints pendientes.

### 2026-09-01 — Versionado y publicación del repositorio

- Por autorización explícita del propietario, se versionó el árbol entregado —producto, documentación, decisiones, manuales y configuración reproducible— en `c3cfeeb` (`feat: completar centro de control y rediseño móvil`) y se publicó sin forzar historial en `origin/master` (`Jotures/rt-sitram-sac-project`).
- Se excluyeron del repositorio los artefactos intermedios de `tmp/` y la caché `__pycache__/`; los manuales PDF finales permanecen versionados como binarios. No se incluyeron secretos: sólo archivos de ejemplo de entorno.

### 2026-09-01 — Recuperación de rutas de producción

- Después del `push`, la integración Git de Vercel generó dos despliegues automáticos con `Root Directory = .`; ambos terminaron Ready en segundos y sustituyeron el alias de producción con un sitio sin artefactos, causando `404 NOT_FOUND` incluso en Inicio y en rutas profundas.
- Se confirmó que no era un error de React, API ni datos: el `vercel.json` correcto estaba en `implementation/` e incluye la reescritura SPA. Se redeplegó esa raíz como `dpl_bK8wAiHM7JRUDHSHLTmFUxEEc1yz`, Ready, y se verificó HTTP 200 para `/`, `/inicio` y `/viajes/68390c84-f554-486d-a376-8c94c617ce21/resumen`.
- Se actualizó la configuración del proyecto Vercel `rt-sitram-centro-control` a `Root Directory = implementation`, por lo que los siguientes despliegues de Git usarán el workspace, la build Vite y la configuración de rutas correctos. No hubo migraciones, cambios de datos, RLS, RPC, secretos ni PowerSync.

### 2026-09-01 — Administración de accesos pendiente de publicación (DEC-043)

- El propietario autorizó implementar gestión de usuarios desde Perfiles. Se aceptó DEC-043: las salidas se resuelven suspendiendo acceso de forma reversible, nunca borrando perfil, Auth ni historial operativo.
- Se añadió el comando `manage_company_profile_access`: sólo Gerencia puede suspender/reactivar, cambiar rol o desvincular Conductor; exige motivo, conserva empresa, rechaza autoedición, protege la última Gerencia activa y bloquea suspensión/desvinculación con viaje activo. Toda acción usa auditoría server-side.
- La UI cambia Perfiles a Accesos, muestra gestión por usuario y permite suspensión/reactivación, cambio de rol, desvinculación y reenvío de invitación. El reenvío se encapsula en `resend-company-invitation`, una Edge Function con validación de sesión, empresa y Gerencia antes de usar el servicio de Auth.
- Pasaron `pnpm format:check`, `pnpm lint`, TypeScript del cliente, `git diff --check` y 39 pruebas unitarias del gateway. El host no tiene Docker/Podman, por lo que no fue posible ejecutar pgTAP local.
- La aplicación remota se bloqueó de forma segura: `supabase db push --linked --dry-run --yes` y `supabase functions deploy resend-company-invitation --use-api` devolvieron 403 de privilegios de plataforma. No se aplicó migración, no se desplegó la Edge Function ni se publicó Vercel, para no enviar una UI que dependa de contratos inexistentes en producción.

### 2026-09-01 — Aplicación remota de DEC-043

- Tras actualizar la sesión local con una cuenta autorizada, el `dry-run` confirmó la única migración pendiente y se aplicó `20260901210749_manage_company_profile_access.sql` en Supabase remoto. La CLI no pudo construir su caché local pg-delta por falta de Docker, pero el remoto confirmó la migración aplicada.
- Se desplegó `resend-company-invitation` por API; `functions list` la confirmó `ACTIVE`, versión 1, con `verify_jwt = true`.
- La consulta remota confirmó que `manage_company_profile_access(uuid,text,app_role,text)` existe, puede ejecutarse desde `authenticated` y sigue inaccesible para `anon`. No se invocó ningún comando de negocio ni se alteraron perfiles, viajes o conductores durante la validación.
- `pnpm format:check`, `pnpm lint`, TypeScript y las 39 pruebas del gateway pasaron. El build web generó el artefacto de producción. El intento de `vercel deploy --prod` desde `implementation/` resolvió erróneamente `implementation/implementation` por el `Root Directory` remoto, por lo que la publicación del cliente se realiza mediante el flujo Git configurado desde la raíz del repositorio.

### 2026-09-01 — Producción de DEC-043

- El primer despliegue Git de `5a8b222` falló antes de servir tráfico: Vercel identificó un `SimpleForm` sin `children` en el formulario de reenvío. No cambió el alias canónico y producción anterior continuó respondiendo 200.
- Se corrigió el requisito de tipado en `699a96a` y se publicó por Git la producción `dpl_38cD5BXbQ4Yt3Y1y1N427i3JHb7p`, Ready en `https://rt-sitram-centro-control-oxzo07rkj-jotures-projects.vercel.app`. El alias canónico quedó asociado a esta versión.
- El humo HTTP confirmó 200 en `/configuracion/perfiles` e `/inicio`; Vercel no devolvió logs de error para el despliegue. No se realizaron mutaciones de usuarios ni de datos de negocio durante la validación.

### 2026-09-01 — Operación asistida por oficina (DEC-044)

- Se sustituyó DEC-024 por DEC-044: el registro operativo de Conductor sigue siendo obligatorio, pero la cuenta de acceso es opcional. Cada viaje conserva un canal exclusivo de captura: `driver_app` u `staff_assisted`.
- La migración remota `20260901224734_enable_staff_assisted_trip_operations.sql` añadió el modo y fecha de cambio, programación explícita, traspasos auditados, ciclo operativo en línea para Gerencia/Administración, controles de captura previa/offline, restricciones de acceso de conductor y ACL explícita. `anon` no puede ejecutar los nuevos RPC; se mantuvo al actor administrativo separado del conductor asignado.
- Se regeneraron los tipos desde Supabase, se actualizaron PowerSync y las interfaces de Administración/Conductor. Viajes permite programar a un conductor sin cuenta en modo oficina, gestionar inicio, llegada, carga/vacío, kilometraje, incidencia, cierre y devolución a la app; la interfaz del Conductor conserva el viaje en lectura con explicación cuando la oficina controla la captura.
- Las pruebas SQL remotas pasaron 18/18 para programación y ciclo sintético sin app, y 89/89 para contratos y ACL. Todas se ejecutaron en transacciones con `ROLLBACK`; no dejaron datos de negocio. El advisor de Supabase informó advertencias preexistentes de otros comandos/políticas, sin una exposición nueva de estos RPC.
- Las validaciones locales pasaron: formato, lint, typecheck, build productivo y 288 pruebas en 61 archivos. La compilación conserva la advertencia informativa preexistente de chunks grandes.
- El commit `980dfc0` se publicó en `master` y activó el despliegue de Vercel `dpl_CvmFWNQPrk8wnWiqVigwiP9fgNmY`, Ready. El alias canónico quedó en `https://rt-sitram-centro-control.vercel.app`. La verificación autenticada mostró el selector de modo, el conductor sin app etiquetado como operación desde oficina y la consola operativa; no hubo desborde horizontal en 320, 390, 639, 960 ni 1440 px. No se ejecutaron acciones reales de viaje.

### 2026-09-03 — Peso y flete diferidos (DEC-045)

- El propietario confirmó que una unidad puede salir de Cusco, recibir combustible y viáticos, avanzar hasta un punto de carga y terminar su operación sin conocer todavía toneladas ni flete. Se aceptó DEC-045: `Origen` es salida, `Punto de carga` es opcional y `Destino` es entrega; condiciones comerciales usan `NULL`, no cero.
- Se aplicó en Supabase remoto la migración `20260903010900_defer_trip_commercial_terms.sql` después del preflight que confirmó 0 viajes, cargas, facturas y facturas activas. Añade `pickup_location`, los RPC `create_trip_draft` y `set_trip_commercial_terms`, auditoría, control de versión, bloqueo por factura activa/cancelación y el endurecimiento de facturación. Las escrituras directas de cliente sobre viajes y cargas quedaron revocadas.
- Se desplegó la configuración de PowerSync con `pickup_location`; el validador y la réplica inicial terminaron correctamente y el estado remoto reportó `connected`, sin errores y lag cero. El conductor conserva operación offline y ve la ruta completa; la edición comercial sigue online-only.
- La cobertura nueva de pgTAP para condiciones comerciales pasó 41/41 en remoto y `backend_invariants.test.sql` pasó tras actualizar sus fixtures de eventos offline para respetar la marca temporal de canal de captura. Una suite histórica aislada de odómetro GPS continuó con seis fallos preexistentes no relacionados; no se mezcló su corrección en DEC-045.
- El cliente incorpora creación pendiente, importe total o tarifa por tonelada diferidos, tarjeta de estado comercial, edición para Gerencia/Administración, facturación explicablemente bloqueada, reportes sin ceros falsos y tipos/esquema local actualizados. Antes de cerrar, faltan las validaciones completas locales, el despliegue Git/Vercel y el humo autenticado del flujo principal.

## Handoff

El resultado central está en
`docs/analisis_operativo/corpus_whatsapp_ingesta_20260828/17_matriz_cobertura_adaptacion_aplicativo.md`;
los casos y el procedimiento están en los documentos 16 y 18. DEC-037/P1,
DEC-038/P2, DEC-039, DEC-040 y DEC-041 están desplegados técnicamente, con
migraciones, pgTAP, ACL, lint, build y humo público verificados. DEC-042 queda
integrada y verificada sólo de forma local; no se desplegó ni alteró la
infraestructura. Esto no reemplaza el UAT representativo ni el corte de datos
de DEC-027. No declarar todavía la adaptación integral ni registrar hechos
reales de forma masiva. El siguiente
paso es ejecutar UAT autenticado con datos sintéticos para los cuatro roles y
los breakpoints 320/390/639/960/escritorio, incluido el recorrido offline de
Conductor; después, aprobar o delimitar los hallazgos, etiquetar el estado
revisado, decidir el retiro o reemplazo de los datos sintéticos y
realizar el corte controlado. No migrar mensajes, saldos, comprobantes ni alias
al backend sin validación y autorización independientes.
